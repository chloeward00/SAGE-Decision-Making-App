import Head from 'next/head'
import 'bootstrap/dist/css/bootstrap.min.css';

export default function Home() {
  return ('If you put Create-Account in the url you will get to the create account page and login in the url too, once logged in im working on delete account atm so put Delete-Acccount into the url like this /dashboard/Delete-Account')
}
